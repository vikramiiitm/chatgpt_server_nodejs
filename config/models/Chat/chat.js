class Conversation {
    constructor(user, topic, title) {
        this.user = user;
        this.topic = topic;
        this.title = title
    }
}

class Message {
    constructor(
        conversation, 
        user, 
        message, 
        messasges, 
        tokens, 
        is_bot, 
        is_disabled, 
        message_type, 
        embedding_message_doc,
        plain_message_type){

            this.conversation
    }
}

module.exports = {
    Conversation
};